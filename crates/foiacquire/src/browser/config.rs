//! Browser engine configuration re-exports.
//!
//! Type definitions now live in `crate::config::browser` so they are
//! always available regardless of the `browser` feature flag.

pub use crate::config::browser::*;
